[project]
name = "enrichment-worker"
version = "0.1.0"
description = "Token enrichment + scoring using hermes-token-screener algorithms"
requires-python = ">=3.13"
dependencies = [
    "numpy",
    "httpx",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
